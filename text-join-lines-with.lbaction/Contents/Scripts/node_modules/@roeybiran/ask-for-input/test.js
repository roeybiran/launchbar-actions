"use strict";

const ask = require("./index");

(async () => {
  const foo = await ask({
    "text": "foo",
    "title": "bar",
    "buttons": ["quux", "biz", "baz"],
    "cancelButton": "biz",
    "defaultButton": "quux",

  });
  console.log(foo);
})();
