"use strict";

const lbEnv = require("@roeybiran/launchbar-env");
const Conf = require("conf");

/**
 * Persist data in the respective action's "support" directory.
 */
module.exports = new Conf({
  cwd: lbEnv.supportPath
});
