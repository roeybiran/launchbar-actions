"use strict";

const lbenv = require("@roeybiran/launchbar-env");
const CacheConf = require("cache-conf");

/**
 * Caches an item in the respective action's cache directory.
 */
module.exports = new CacheConf({
  configName: "cache",
  cwd: lbenv.cachePath
});
