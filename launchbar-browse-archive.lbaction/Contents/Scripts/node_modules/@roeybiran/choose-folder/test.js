"use strict";

const choose = require("./index");

(async () => {
  const foo = await choose({
    prompt: "foo",
    defaultLocation: "/Users/roey/",
    showInvisibles: true
  });
  console.log(foo);
})();
